repos:
  - repo: local
    hooks:
      - id: project-check
        name: project check
        entry: env ARTIGAS_SKIP_HOOK_INSTALL=1 bash scripts/check.sh
        language: system
        pass_filenames: false
        stages: [pre-commit]
