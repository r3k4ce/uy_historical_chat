repos:
  - repo: local
    hooks:
      - id: project-check
        name: project check
        entry: bash scripts/check.sh
        language: system
        pass_filenames: false
        stages: [pre-push]
